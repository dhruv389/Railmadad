# Server Port (Render automatically sets PORT to 10000 or similar, default: 5000)
PORT=5000

# MongoDB Connection URI
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# JWT Secret Key
SECRET_KEY=your_jwt_secret_key_here

# Frontend Client URL (Set this to your Vercel frontend URL in production, e.g., https://your-app.vercel.app)
CLIENT_URL=http://localhost:5173

# Email Service Credentials for OTPs (Gmail App Password)
EMAIL_USER=drcoder389@gmail.com
EMAIL_PASS=vmam tqmr pkei ekqb
